[pytest]
# The Python test harnesses (tests/contract, tests/e2e) import each other as `tests.…` from the repo
# root. They judge the .NET server from outside and never import it.
pythonpath = .
markers =
    weir_e2e: Weir React/API spine smoke (opt-in: WEIR_E2E=1; see tests/e2e/weir/)
