.venv/
venv/
__pycache__/
*.py[cod]
*.sqlite3
*.db
*.log

node_modules/
dist/
.vite/

# Python test runners (tests/contract, tests/e2e)
.pytest_cache/
pytest-cache-files-*/
.ruff_cache/

.env
.env.*
!.env.example

.DS_Store
Thumbs.db

.idea/
.vscode/
*.code-workspace

# Local-only working files (never commit)
.local/
.local-dev-home/
.local-dev-home*/
.tmp/

# Script-generated UI captures (see scripts/capture-overview-sections.cjs)
artifacts/

# Local scratch (screenshots, one-off captures, ad-hoc Playwright installs)
tmp/
scripts/node_modules/
apps/web/tmp-*.cjs

# Build-time downloaded Windows FFmpeg runtime
packaging/windows/vendor/

# Claude Code local settings (machine-specific, never commit)
.claude/settings.local.json

# .NET build output. apps/tray/Weir.Tray had bin/ and obj/ committed — 29 files,
# Debug binaries and caches — so any `dotnet build` dirtied the tree and put compiled
# artifacts in review diffs. Nothing reads them: build-velopack.ps1 runs `dotnet publish`
# from source, in Release, into its own output directory.
apps/tray/*/bin/
apps/tray/*/obj/
apps/server/**/bin/
apps/server/**/obj/
apps/server/**/TestResults/
apps/server/artifacts/

# Docusaurus
docs-site/build/
docs-site/.docusaurus/

# Scratch WEIR_CONTRACT_LEDGER files from local contract-suite runs
.contract-ledger/
