"""HTTP routes under ``/api/v1/system`` for suite configuration bundles and backup files.

Mirrors also exist under ``/api/v1/suite/…`` (see :mod:`mediamop.platform.suite_settings.router`) for deployments
whose reverse proxies only allow the ``/suite/*`` prefix.
"""
