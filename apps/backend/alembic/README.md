Revision scripts live in **`versions/`**.

- **`0001_initial_auth`** — `users`, `user_sessions` (PostgreSQL).

Run from **`apps/backend`** with `PYTHONPATH=src` and **`MEDIAMOP_DATABASE_URL`** set (see parent `README.md`).
